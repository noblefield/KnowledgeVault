# Backend local development env (not used by Docker)
# Adjust values to match your docker-compose Postgres mapping

# Example: if POSTGRES_PORT in docker-compose is 5432
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/postgres

# Security
SECRET_KEY=change_me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# LLM / Embeddings (optional for local dev)
VOYAGE_API_KEY=
OPENAI_API_KEY=

# App
BACKEND_PORT=8000
